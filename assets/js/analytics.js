/* ============================================================
   ICU Helper — analytics.js
   Vercel Web Analytics Integration
   ============================================================ */
import { inject } from '@vercel/analytics';

// Initialize Vercel Web Analytics
inject();
